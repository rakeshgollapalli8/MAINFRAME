-- ********************************************************************
-- *** FOREIGN KEY
-- ********************************************************************

-- --- DROP TABLES ----------------------------------------------------
--SDROP TABLE SUPPLIER;
   DROP TABLE PRODUCT;
   COMMIT;

-- --- CREATE MASTER TABLE (SUPPLIER) ------------------------------
   CREATE TABLE SUPPLIER
      (
          SUPPL_CODE  SMALLINT      NOT NULL      PRIMARY KEY,
          SUPPL_NAME  CHAR(10)      NOT NULL
      ) IN OZA194DB.OZA194TS;
   CREATE UNIQUE INDEX IDX_35 ON SUPPLIER(SUPPL_CODE);

-- --- CREATE CHILD TABLE (PRODUCT)------------------------------------
   CREATE TABLE PRODUCT
      (
          PROD_CODE      SMALLINT     NOT NULL      PRIMARY KEY,
          PROD_NAME      CHAR(10)     NOT NULL,
          SUPPL_CODE     SMALLINT,
          CONSTRAINT     CONST03      FOREIGN KEY(SUPPL_CODE)
                                      REFERENCES SUPPLIER(SUPPL_CODE)
                                      ON DELETE CASCADE
      ) IN OZA194DB.OZA194TS;
   CREATE UNIQUE INDEX IDX_36 ON PRODUCT(PROD_CODE);
   COMMIT;

-- --- INSERT ROWS IN MASTER TABLE (SUPPLIER)--------------------------
   INSERT INTO SUPPLIER VALUES(100,'RAKESH');
   INSERT INTO SUPPLIER VALUES(101,'ANIL');
   INSERT INTO SUPPLIER VALUES(102,'RAMESH');
   COMMIT;

-- --- INSERT ROWS IN CHILD TABLE (PRODUCT)----------------------------
   INSERT INTO PRODUCT VALUES(200,'MOUSE',100);
   INSERT INTO PRODUCT VALUES(201,'KEYBOAD',101);
   INSERT INTO PRODUCT VALUES(202,'PENDRIVE',101);
   INSERT INTO PRODUCT VALUES(203,'CALCULA',101);
   INSERT INTO PRODUCT VALUES(204,'TALLY',102);
   INSERT INTO PRODUCT VALUES(205,'LAPTOP',102);
   INSERT INTO PRODUCT VALUES(206,'CURSOR',102);
   INSERT INTO PRODUCT VALUES(207,'TABLE',100);
   COMMIT;

-- --- DISPLAY ROWS FROM MASTER AND CHILD TABLE BEFORE DELETE ---------
   SELECT * FROM SUPPLIER;
   SELECT * FROM PRODUCT;

   DELETE FROM SUPPLIER WHERE SUPPL_CODE = '101';

-- --- DISPLAY ROWS FROM MASTER AND CHILD TABLE AFTER DELETE ----------
   SELECT * FROM SUPPLIER;
   SELECT * FROM PRODUCT
